/**
 * Base URL of the render server (see render-server/). Everything in this folder
 * talks to it: nothing else in the editor should fetch it directly.
 */
export const RENDER_SERVER_URL = "http://localhost:4000";
